#include <iostream>
#include <vector>
using namespace std;

int main(void) {
    int n; 
    cin >> n;
    vector<int> d(n);
    for (int i = 0 ; i < n; i++) {
        cin >> d[i];
    }
    for (int a = 0; a < n ; a++) {
        
    }
}
